# Bug Log

A list of bugs found when tinkering with the app

## UI/UX

- ✅ When checking off task, rn you must press title. Bad behavior, should be when pill is pressed
- ✅ Task linked to goal should have link to go to full goal
- Goal active, completed, archived need to give more user feedback e.g archived should have confirmation message, completed should have a little celebration.
- Confirmation needed before subgoals are deleted (similar to deleting a task)
- Update tailwind config and ui so that it follows best practices

## Backend

## DB

- ✅ When goal is deleted, subgoal subtasks still exist just become standalone (user should be prompted if it should still exist)
